# Repo này là GitHub profile — chỉ chứa README.md render lên trang profile.
# Mọi thứ khác (skills, practice, tài liệu CCA-F) nằm ở repo riêng nc-private.

# Secrets — never commit API keys
.env
.env.*
!.env.example
